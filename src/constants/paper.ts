export const defaultPaperSize: { width: number; height: number } = {
  width: 1400,
  height: 1200,
};
export const pageBreakSettings: {
  color: string;
  width: number;
  height: number;
} = {
  color: "#353535",
  width: 1000,
  height: 1000,
};
